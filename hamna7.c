#include <stdio.h>
int main()
{
	double a=2.888889997777;
	printf("before getting the precision number\n number is: %lf \n",a);
	printf("after getting the precision number\n number is: %.14lf \n",a);
}
